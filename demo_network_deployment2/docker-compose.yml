version: '2'
services:

  backend2:
    image: my-host/docker_network-backend2
    networks:
      docker_network_demo:
        aliases:
          - backend2

networks:
  docker_network_demo:
    external:
      name: demonetwork_demo  
